---
// src/components/StickyCall.astro — Sticky "Jab ready ho, call karo" CTA; prominent on mobile
const tel = import.meta.env.PUBLIC_PHONE_NUMBER || '919876543210';
const telLink = `tel:+${tel.replace(/\D/g, '')}`;
---

<div class="fixed bottom-0 left-0 right-0 z-50 border-t border-stone-200 bg-white p-3 shadow-lg md:bottom-4 md:left-auto md:right-4 md:max-w-xs md:rounded-xl">
  <a
    href={telLink}
    class="flex flex-col items-center gap-0.5 rounded-lg bg-emerald-600 px-4 py-3 font-medium text-white no-underline hover:bg-emerald-700"
  >
    <span class="flex items-center gap-2">
      <span aria-hidden="true">📞</span>
      Jab ready ho, call karo
    </span>
    <span class="text-xs font-normal text-emerald-100">Local expert se baat karo</span>
  </a>
</div>
