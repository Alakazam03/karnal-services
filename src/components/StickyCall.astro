---
// src/components/StickyCall.astro — Sticky “Call when ready” CTA; prominent on mobile
const tel = import.meta.env.PUBLIC_PHONE_NUMBER || '919876543210';
const telLink = `tel:+${tel.replace(/\D/g, '')}`;
---

<div class="fixed bottom-0 left-0 right-0 z-50 border-t border-stone-200 bg-white p-3 shadow-lg md:bottom-4 md:left-auto md:right-4 md:max-w-xs md:rounded-xl">
  <a
    href={telLink}
    class="flex items-center justify-center gap-2 rounded-lg bg-emerald-600 px-4 py-3 font-medium text-white no-underline hover:bg-emerald-700"
  >
    <span aria-hidden="true">📞</span>
    Call when ready
  </a>
</div>
